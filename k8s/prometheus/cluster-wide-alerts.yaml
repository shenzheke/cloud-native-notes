# cluster-wide-alerts.yaml
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: cluster-wide-alerts
  namespace: monitoring
  labels:
    release: prometheus
spec:
  groups:

  - name: pod.rules
    rules:
    - alert: PodCrashLooping
      expr: rate(kube_pod_container_status_restarts_total[15m]) * 60 > 0
      for: 5m
      labels:
        severity: warning
      annotations:
        summary: "Pod 频繁重启: {{ $labels.namespace }}/{{ $labels.pod }}"
        description: "容器 {{ $labels.container }} 重启速率 {{ $value | humanize }}/min"

    - alert: PodNotReady
      expr: |
        kube_pod_status_ready{condition="true"} == 0
        and kube_pod_status_phase{phase=~"Running|Pending"} == 1
      for: 5m
      labels:
        severity: warning
      annotations:
        summary: "Pod 未就绪: {{ $labels.namespace }}/{{ $labels.pod }}"

    - alert: PodOOMKilled
      expr: kube_pod_container_status_last_terminated_reason{reason="OOMKilled"} == 1
      for: 1m
      labels:
        severity: critical
      annotations:
        summary: "OOM Kill: {{ $labels.namespace }}/{{ $labels.pod }}"

  - name: node.rules
    rules:
    - alert: NodeHighCPU
      expr: |
        1 - avg by(instance) (rate(node_cpu_seconds_total{mode="idle"}[5m])) > 0.85
      for: 5m
      labels:
        severity: warning
      annotations:
        summary: "节点 CPU 过高: {{ $labels.node }}"
        description: "使用率 {{ $value | humanizePercentage }}"

    - alert: NodeHighMemory
      expr: |
        1 - (node_memory_MemAvailable_bytes / node_memory_MemTotal_bytes) > 0.85
      for: 5m
      labels:
        severity: warning
      annotations:
        summary: "节点内存过高: {{ $labels.instance }}"
        description: "使用率 {{ $value | humanizePercentage }}"

    - alert: NodeDiskPressure
      expr: |
        (node_filesystem_avail_bytes{fstype!="tmpfs"} / node_filesystem_size_bytes) < 0.15
      for: 5m
      labels:
        severity: critical
      annotations:
        summary: "磁盘空间紧张: {{ $labels.instance }} {{ $labels.mountpoint }}"
        description: "剩余 {{ $value | humanizePercentage }}"

  - name: otel-demo.rules
    rules:
    - alert: OtelDemoHighErrorRate
      expr: |
        sum(rate(traces_spanmetrics_calls_total{
          span_kind="SPAN_KIND_SERVER",
          status_code="STATUS_CODE_ERROR",
          namespace="otel-demo"
        }[5m]))
        /
        sum(rate(traces_spanmetrics_calls_total{
          span_kind="SPAN_KIND_SERVER",
          namespace="otel-demo"
        }[5m])) > 0.05
      for: 2m
      labels:
        severity: warning
        namespace: otel-demo
      annotations:
        summary: "otel-demo 错误率 > 5%"
        description: "当前错误率 {{ $value | humanizePercentage }}"

    - alert: OtelDemoServiceDown
      expr: up{namespace="otel-demo"} == 0
      for: 1m
      labels:
        severity: critical
        namespace: otel-demo
      annotations:
        summary: "服务下线: {{ $labels.job }}"
